{
  "name": "npm-esbuild-audit",
  "version": "0.1.0",
  "description": "audit only required packages",
  "main": "dist/index.js",
  "type": "module",
  "bin": "dist/index.js",
  "scripts": {
    "prepare": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --loader ts-node/esm/transpile-only src/index.ts --target esnext --platform node --format esm src/index.ts"
  },
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "@npmcli/arborist": "^3.0.0",
    "commander": "^8.2.0",
    "esbuild": "^0.13.4"
  },
  "files": [ "dist/" ]
}
